# Structured Specification Template
You can use edit this document to add your own materials. 

## Goal
Describe in 1–2 sentences the objective of the meeting slot suggestion system.

## Inputs and Outputs
- **Inputs:** Describe each input parameter (`events`, `meeting_duration`, `day`). What type and format does each take? What values are valid?
- **Output:** Describe what the function returns.

## Acceptance Criteria (Given–When–Then)
Provide at least **six** acceptance criteria using the Given–When–Then format. For example:

- *Given* the user provides no existing events, *when* the meeting duration is 30 minutes on a Monday, *then* the earliest suggested slot is 09:00.

Write your own criteria to cover various cases in the system specification.

## Constraints and Assumptions
List at least **six** constraints and assumptions relevant to the problem. 

